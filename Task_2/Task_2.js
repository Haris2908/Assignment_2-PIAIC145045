var trafiicLight = prompt("Enter the traffic lights color")

if (trafiicLight === "Red"){
    alert("Must stop")
}

if (trafiicLight === "Yellow"){
    alert("Ready to move")
}

if (trafiicLight === "Green"){
    alert("Move now")
}